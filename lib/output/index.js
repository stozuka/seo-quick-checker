'use strict';

const { toConsoleLog } = require('./to-console-log');
const { toFile } = require('./to-file');
const { toStream } = require('./to-stream');

module.exports = {
  toConsoleLog,
  toFile,
  toStream,
};

'use strict';

const { toConsole } = require('./to-console');
const { toFile } = require('./to-file');
const { toStream } = require('./to-stream');

module.exports = {
  toConsole,
  toFile,
  toStream,
};
